MASAGI landing — partner logos
================================

The homepage trust strip ("Trusted by lubricant & energy companies across
Indonesia") shows these six partner logos. Drop the PNG files here with these
exact names and they appear automatically — until then, the strip shows clean
text wordmarks as a fallback (no broken images):

  logo-maxima.png
  logo-shell.png
  logo-repsol.png
  logo-united-oil.png
  logo-lemigas.png
  logo-evalube.png

Recommended: transparent-background PNGs, ~120–150 px wide. The tiles apply a
grayscale filter that lifts to full colour on hover, matching the design.
